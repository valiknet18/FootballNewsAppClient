(function () {
    angular
        .module('football.controller.commands')
        .controller('CommandCtrl', CommandCtrl)
    ;

    CommandCtrl.$inject = [
        '$scope'
    ];

    //Command Ctrl block
    function CommandCtrl ($scope) {

    }
})();